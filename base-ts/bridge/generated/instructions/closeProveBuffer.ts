/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BRIDGE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_PROVE_BUFFER_DISCRIMINATOR = new Uint8Array([
  79, 14, 144, 49, 3, 124, 210, 74,
]);

export function getCloseProveBufferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_PROVE_BUFFER_DISCRIMINATOR
  );
}

export type CloseProveBufferInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountProveBuffer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountProveBuffer extends string
        ? WritableAccount<TAccountProveBuffer>
        : TAccountProveBuffer,
      ...TRemainingAccounts,
    ]
  >;

export type CloseProveBufferInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseProveBufferInstructionDataArgs = {};

export function getCloseProveBufferInstructionDataEncoder(): FixedSizeEncoder<CloseProveBufferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_PROVE_BUFFER_DISCRIMINATOR })
  );
}

export function getCloseProveBufferInstructionDataDecoder(): FixedSizeDecoder<CloseProveBufferInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseProveBufferInstructionDataCodec(): FixedSizeCodec<
  CloseProveBufferInstructionDataArgs,
  CloseProveBufferInstructionData
> {
  return combineCodec(
    getCloseProveBufferInstructionDataEncoder(),
    getCloseProveBufferInstructionDataDecoder()
  );
}

export type CloseProveBufferInput<
  TAccountOwner extends string = string,
  TAccountProveBuffer extends string = string,
> = {
  /**
   * The account paying for the transaction fees and receiving the rent back.
   * It must be the owner of the prove buffer account.
   */
  owner: TransactionSigner<TAccountOwner>;
  /** The prove buffer account to close */
  proveBuffer: Address<TAccountProveBuffer>;
};

export function getCloseProveBufferInstruction<
  TAccountOwner extends string,
  TAccountProveBuffer extends string,
  TProgramAddress extends Address = typeof BRIDGE_PROGRAM_ADDRESS,
>(
  input: CloseProveBufferInput<TAccountOwner, TAccountProveBuffer>,
  config?: { programAddress?: TProgramAddress }
): CloseProveBufferInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountProveBuffer
> {
  // Program address.
  const programAddress = config?.programAddress ?? BRIDGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    proveBuffer: { value: input.proveBuffer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.proveBuffer),
    ],
    data: getCloseProveBufferInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseProveBufferInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountProveBuffer
  >);
}

export type ParsedCloseProveBufferInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The account paying for the transaction fees and receiving the rent back.
     * It must be the owner of the prove buffer account.
     */
    owner: TAccountMetas[0];
    /** The prove buffer account to close */
    proveBuffer: TAccountMetas[1];
  };
  data: CloseProveBufferInstructionData;
};

export function parseCloseProveBufferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseProveBufferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), proveBuffer: getNextAccount() },
    data: getCloseProveBufferInstructionDataDecoder().decode(instruction.data),
  };
}
