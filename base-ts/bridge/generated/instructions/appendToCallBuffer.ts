/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BRIDGE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const APPEND_TO_CALL_BUFFER_DISCRIMINATOR = new Uint8Array([
  113, 115, 232, 194, 248, 32, 39, 21,
]);

export function getAppendToCallBufferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPEND_TO_CALL_BUFFER_DISCRIMINATOR
  );
}

export type AppendToCallBufferInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountCallBuffer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountCallBuffer extends string
        ? WritableAccount<TAccountCallBuffer>
        : TAccountCallBuffer,
      ...TRemainingAccounts,
    ]
  >;

export type AppendToCallBufferInstructionData = {
  discriminator: ReadonlyUint8Array;
  data: ReadonlyUint8Array;
};

export type AppendToCallBufferInstructionDataArgs = {
  data: ReadonlyUint8Array;
};

export function getAppendToCallBufferInstructionDataEncoder(): Encoder<AppendToCallBufferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['data', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: APPEND_TO_CALL_BUFFER_DISCRIMINATOR,
    })
  );
}

export function getAppendToCallBufferInstructionDataDecoder(): Decoder<AppendToCallBufferInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['data', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getAppendToCallBufferInstructionDataCodec(): Codec<
  AppendToCallBufferInstructionDataArgs,
  AppendToCallBufferInstructionData
> {
  return combineCodec(
    getAppendToCallBufferInstructionDataEncoder(),
    getAppendToCallBufferInstructionDataDecoder()
  );
}

export type AppendToCallBufferInput<
  TAccountOwner extends string = string,
  TAccountCallBuffer extends string = string,
> = {
  /**
   * The signer authorized to modify this call buffer.
   * Must match `call_buffer.owner`.
   */
  owner: TransactionSigner<TAccountOwner>;
  /**
   * The call buffer account to append data to.
   * Must have been initialized with enough space to hold the resulting
   * data; this instruction does not reallocate and will revert if
   * serialization would exceed the account's allocated size.
   */
  callBuffer: Address<TAccountCallBuffer>;
  data: AppendToCallBufferInstructionDataArgs['data'];
};

export function getAppendToCallBufferInstruction<
  TAccountOwner extends string,
  TAccountCallBuffer extends string,
  TProgramAddress extends Address = typeof BRIDGE_PROGRAM_ADDRESS,
>(
  input: AppendToCallBufferInput<TAccountOwner, TAccountCallBuffer>,
  config?: { programAddress?: TProgramAddress }
): AppendToCallBufferInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountCallBuffer
> {
  // Program address.
  const programAddress = config?.programAddress ?? BRIDGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    callBuffer: { value: input.callBuffer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.callBuffer),
    ],
    data: getAppendToCallBufferInstructionDataEncoder().encode(
      args as AppendToCallBufferInstructionDataArgs
    ),
    programAddress,
  } as AppendToCallBufferInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCallBuffer
  >);
}

export type ParsedAppendToCallBufferInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The signer authorized to modify this call buffer.
     * Must match `call_buffer.owner`.
     */
    owner: TAccountMetas[0];
    /**
     * The call buffer account to append data to.
     * Must have been initialized with enough space to hold the resulting
     * data; this instruction does not reallocate and will revert if
     * serialization would exceed the account's allocated size.
     */
    callBuffer: TAccountMetas[1];
  };
  data: AppendToCallBufferInstructionData;
};

export function parseAppendToCallBufferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedAppendToCallBufferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), callBuffer: getNextAccount() },
    data: getAppendToCallBufferInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
