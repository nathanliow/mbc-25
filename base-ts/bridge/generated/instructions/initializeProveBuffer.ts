/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from '@solana/kit';
import { BRIDGE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INITIALIZE_PROVE_BUFFER_DISCRIMINATOR = new Uint8Array([
  20, 200, 91, 188, 138, 211, 253, 222,
]);

export function getInitializeProveBufferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PROVE_BUFFER_DISCRIMINATOR
  );
}

export type InitializeProveBufferInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountBridge extends string | AccountMeta<string> = string,
  TAccountProveBuffer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountBridge extends string
        ? ReadonlyAccount<TAccountBridge>
        : TAccountBridge,
      TAccountProveBuffer extends string
        ? WritableSignerAccount<TAccountProveBuffer> &
            AccountSignerMeta<TAccountProveBuffer>
        : TAccountProveBuffer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeProveBufferInstructionData = {
  discriminator: ReadonlyUint8Array;
  maxDataLen: bigint;
  maxProofLen: bigint;
};

export type InitializeProveBufferInstructionDataArgs = {
  maxDataLen: number | bigint;
  maxProofLen: number | bigint;
};

export function getInitializeProveBufferInstructionDataEncoder(): FixedSizeEncoder<InitializeProveBufferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['maxDataLen', getU64Encoder()],
      ['maxProofLen', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_PROVE_BUFFER_DISCRIMINATOR,
    })
  );
}

export function getInitializeProveBufferInstructionDataDecoder(): FixedSizeDecoder<InitializeProveBufferInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['maxDataLen', getU64Decoder()],
    ['maxProofLen', getU64Decoder()],
  ]);
}

export function getInitializeProveBufferInstructionDataCodec(): FixedSizeCodec<
  InitializeProveBufferInstructionDataArgs,
  InitializeProveBufferInstructionData
> {
  return combineCodec(
    getInitializeProveBufferInstructionDataEncoder(),
    getInitializeProveBufferInstructionDataDecoder()
  );
}

export type InitializeProveBufferInput<
  TAccountPayer extends string = string,
  TAccountBridge extends string = string,
  TAccountProveBuffer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Payer funds the buffer account creation */
  payer: TransactionSigner<TAccountPayer>;
  /** Bridge for pause checks (future use); also a consistent pattern like call buffers */
  bridge: Address<TAccountBridge>;
  /** Prove buffer to be created with capacity sized by the provided max lengths */
  proveBuffer: TransactionSigner<TAccountProveBuffer>;
  systemProgram?: Address<TAccountSystemProgram>;
  maxDataLen: InitializeProveBufferInstructionDataArgs['maxDataLen'];
  maxProofLen: InitializeProveBufferInstructionDataArgs['maxProofLen'];
};

export function getInitializeProveBufferInstruction<
  TAccountPayer extends string,
  TAccountBridge extends string,
  TAccountProveBuffer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BRIDGE_PROGRAM_ADDRESS,
>(
  input: InitializeProveBufferInput<
    TAccountPayer,
    TAccountBridge,
    TAccountProveBuffer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeProveBufferInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountBridge,
  TAccountProveBuffer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BRIDGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    bridge: { value: input.bridge ?? null, isWritable: false },
    proveBuffer: { value: input.proveBuffer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.bridge),
      getAccountMeta(accounts.proveBuffer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeProveBufferInstructionDataEncoder().encode(
      args as InitializeProveBufferInstructionDataArgs
    ),
    programAddress,
  } as InitializeProveBufferInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountBridge,
    TAccountProveBuffer,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeProveBufferInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Payer funds the buffer account creation */
    payer: TAccountMetas[0];
    /** Bridge for pause checks (future use); also a consistent pattern like call buffers */
    bridge: TAccountMetas[1];
    /** Prove buffer to be created with capacity sized by the provided max lengths */
    proveBuffer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitializeProveBufferInstructionData;
};

export function parseInitializeProveBufferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeProveBufferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      bridge: getNextAccount(),
      proveBuffer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeProveBufferInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
