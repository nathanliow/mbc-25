/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BASE_RELAYER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getGasConfigDecoder,
  getGasConfigEncoder,
  type GasConfig,
  type GasConfigArgs,
} from '../types';

export const SET_GAS_CONFIG_DISCRIMINATOR = new Uint8Array([
  244, 68, 172, 138, 187, 145, 160, 192,
]);

export function getSetGasConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_GAS_CONFIG_DISCRIMINATOR
  );
}

export type SetGasConfigInstruction<
  TProgram extends string = typeof BASE_RELAYER_PROGRAM_ADDRESS,
  TAccountCfg extends string | AccountMeta<string> = string,
  TAccountGuardian extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCfg extends string ? WritableAccount<TAccountCfg> : TAccountCfg,
      TAccountGuardian extends string
        ? ReadonlySignerAccount<TAccountGuardian> &
            AccountSignerMeta<TAccountGuardian>
        : TAccountGuardian,
      ...TRemainingAccounts,
    ]
  >;

export type SetGasConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  gasConfig: GasConfig;
};

export type SetGasConfigInstructionDataArgs = { gasConfig: GasConfigArgs };

export function getSetGasConfigInstructionDataEncoder(): FixedSizeEncoder<SetGasConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['gasConfig', getGasConfigEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_GAS_CONFIG_DISCRIMINATOR })
  );
}

export function getSetGasConfigInstructionDataDecoder(): FixedSizeDecoder<SetGasConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['gasConfig', getGasConfigDecoder()],
  ]);
}

export function getSetGasConfigInstructionDataCodec(): FixedSizeCodec<
  SetGasConfigInstructionDataArgs,
  SetGasConfigInstructionData
> {
  return combineCodec(
    getSetGasConfigInstructionDataEncoder(),
    getSetGasConfigInstructionDataDecoder()
  );
}

export type SetGasConfigInput<
  TAccountCfg extends string = string,
  TAccountGuardian extends string = string,
> = {
  /** The bridge account containing configuration */
  cfg: Address<TAccountCfg>;
  /** The guardian account authorized to update configuration */
  guardian: TransactionSigner<TAccountGuardian>;
  gasConfig: SetGasConfigInstructionDataArgs['gasConfig'];
};

export function getSetGasConfigInstruction<
  TAccountCfg extends string,
  TAccountGuardian extends string,
  TProgramAddress extends Address = typeof BASE_RELAYER_PROGRAM_ADDRESS,
>(
  input: SetGasConfigInput<TAccountCfg, TAccountGuardian>,
  config?: { programAddress?: TProgramAddress }
): SetGasConfigInstruction<TProgramAddress, TAccountCfg, TAccountGuardian> {
  // Program address.
  const programAddress = config?.programAddress ?? BASE_RELAYER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cfg: { value: input.cfg ?? null, isWritable: true },
    guardian: { value: input.guardian ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.cfg), getAccountMeta(accounts.guardian)],
    data: getSetGasConfigInstructionDataEncoder().encode(
      args as SetGasConfigInstructionDataArgs
    ),
    programAddress,
  } as SetGasConfigInstruction<TProgramAddress, TAccountCfg, TAccountGuardian>);
}

export type ParsedSetGasConfigInstruction<
  TProgram extends string = typeof BASE_RELAYER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The bridge account containing configuration */
    cfg: TAccountMetas[0];
    /** The guardian account authorized to update configuration */
    guardian: TAccountMetas[1];
  };
  data: SetGasConfigInstructionData;
};

export function parseSetGasConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetGasConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { cfg: getNextAccount(), guardian: getNextAccount() },
    data: getSetGasConfigInstructionDataDecoder().decode(instruction.data),
  };
}
